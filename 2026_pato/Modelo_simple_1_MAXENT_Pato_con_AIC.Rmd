---
title: "Modelo simple 1A Pato (Bioclim 19 + envirem 18 + dist + factorK)"
author: "Viacheslav Shalsiko"
date: "`r Sys.Date()`"
output: html_document
---

## Definir variables

```{r}
file_prefix <- "run1A_maxent"
taxon <- "Dendrocygna autumnalis"
taxon_ <- "Dendrocygna_autumnalis"
predictors <- 1:42
n_fondo <- 5000
predictor_names <- c(paste0('b',1:19),paste0('e',1:18),"d1","d2","d5","d15","FactK","elev")
```

## Preparar el entorno

```{r bibliotecas}
library(sp)
#library(rgdal)
library(raster)
library(dismo)
library(rJava)
library(rworldmap)
library(rworldxtra)
library(rmaxent)
library(usdm)
library(lattice)


sessionInfo()
```

## Cargar puntos de presencia

```{r puntos-presencia}
ocurrencias <- read.csv("../Especies_ejemplos/Dendrocygna_autumnalis.csv")
#ocurrencias <- read.delim("../Especies_ejemplos/ocurrence/0011899-240425142415019.csv")
dim(ocurrencias)
#ocurrencias <- ocurrencias[,c("Species","Long","Lat")]
#ocurrencias <- ocurrencias[,c("species","decimalLongitude","decimalLatitude")]

names(ocurrencias) <- c("species","decimalLongitude","decimalLatitude")
ocurrencias$species <- as.factor(ocurrencias$species)
head(ocurrencias)
levels(ocurrencias$species)

ocurrencias <- ocurrencias[ocurrencias$species == taxon,]
dim(ocurrencias)
```

## Visualizar Puntos de presencia

```{r visualizar-presencias}
mundo <- getMap(resolution = "high")
plot(mundo, axes = TRUE, xlim = c(-107,-100), ylim = c(18.5,23))
points(ocurrencias$decimalLongitude, ocurrencias$decimalLatitude, cex = 0.5, col = "red")
```

## Cargar datos ambientales

```{r predictores-ambientales, cache = TRUE}
bio_vars <- stack("../capas_6110_asc_maxent/bio_asc_dist__43vars.tif")
names(bio_vars) <- predictor_names
bio_vars

plot(bio_vars, c(1:6), col = grey(1:100/100), nc=3)
plot(bio_vars, c(7:12), col = grey(1:100/100), nc=3)
plot(bio_vars, c(13:18), col = grey(1:100/100), nc=3)
plot(bio_vars, c(19:24), col = grey(1:100/100), nc=3)
plot(bio_vars, c(25:30), col = grey(1:100/100), nc=3)
plot(bio_vars, c(31:36), col = grey(1:100/100), nc=3)
plot(bio_vars, c(37:42), col = grey(1:100/100), nc=3)

par(mfcol=c(1,2))

mask_reclass_table <- matrix(c(-Inf, 0, NA, 0, Inf, 1), ncol=3, byrow=TRUE)
## in CHELSA dataset the layer BIO12 cannot be used to produce mask
# bio_vars_mask <- reclassify(subset(bio_vars, 12), mask_reclass_table)
bio_vars_mask <- rasterize(mundo, subset(bio_vars,1))
bio_vars_mask <- reclassify(bio_vars_mask, mask_reclass_table)
plot(bio_vars_mask, legend = FALSE, main = "Continent")

points_raster <- rasterize(ocurrencias[,2:3], bio_vars_mask, 
          rep(1, nrow(ocurrencias)))
buffer_raster <- buffer(points_raster, width = 100000, dissolve = TRUE)
#raster_distancias <- distanceFromPoints(bio_vars_mask, ocurrencias[,2:3]) 
buffer_raster <- bio_vars_mask * buffer_raster
plot(buffer_raster, legend = FALSE, main = "Potential region")
```

## Guardar rasters auxiliares

```{r guardar-raster-auxiliar}
writeRaster(bio_vars_mask,
               filename = paste0("rasters/mascara_continente_",taxon_,".tif"),
              format="GTiff", datatype = "INT2S", overwrite = TRUE)
writeRaster(buffer_raster,
               filename = paste0("rasters/buffer_500km_",taxon_,".tif"),
              format="GTiff", datatype = "INT2S", overwrite = TRUE)
```

## Seleccionar solo los puntos en la zona donde existen datos ambientales

```{r}
print(extent(bio_vars_mask))
mask_extent <- extent(bio_vars_mask)

ocurrencias <- ocurrencias[ocurrencias$decimalLatitude > mask_extent[3] &
                           ocurrencias$decimalLatitude < mask_extent[4] &
                           ocurrencias$decimalLongitude > mask_extent[1] &
                           ocurrencias$decimalLongitude < mask_extent[2],]

```


```{r puntos-presencia-simulados}
## solo se usa con número de puntos bajo
unidad <- 0.00833
## point simulation
ocurrencias_left <- data.frame(species = ocurrencias$species,
                               decimalLongitude = ocurrencias$decimalLongitude - unidad,
                               decimalLatitude = ocurrencias$decimalLatitude)
ocurrencias_rigth <- data.frame(species = ocurrencias$species,
                               decimalLongitude = ocurrencias$decimalLongitude + unidad,
                               decimalLatitude = ocurrencias$decimalLatitude)
ocurrencias_up <- data.frame(species = ocurrencias$species,
                               decimalLongitude = ocurrencias$decimalLongitude,
                               decimalLatitude = ocurrencias$decimalLatitude + unidad)
ocurrencias_bottom <- data.frame(species = ocurrencias$species,
                               decimalLongitude = ocurrencias$decimalLongitude,
                               decimalLatitude = ocurrencias$decimalLatitude - unidad)
#ocurrencias <- rbind(ocurrencias, 
#                     ocurrencias_left,
#                     ocurrencias_rigth,
#                     ocurrencias_up,
#                     ocurrencias_bottom)
```


## Definir conjuntos de puntos de entrenamiento y de control

```{r visualizacion, fig.width = 8, fig.height = 8}
set.seed(1)

fold <- kfold(ocurrencias, k = 5)
ocurrencias_entrenamiento <- ocurrencias[ fold != 1, ]
ocurrencias_control <- ocurrencias[ fold == 1, ]
dim(ocurrencias_entrenamiento)
dim(ocurrencias_control)

## eliminar celdas repetidas en entrenamiento y control
ocurrencias_entrenamiento <- ocurrencias_entrenamiento[!duplicated(
                raster::cellFromXY(bio_vars_mask, ocurrencias_entrenamiento[,2:3])),]
ocurrencias_control <- ocurrencias_control[!duplicated(
                raster::cellFromXY(bio_vars_mask, ocurrencias_control[,2:3])),]
dim(ocurrencias_entrenamiento)
dim(ocurrencias_control)

plot(mundo, axes = TRUE, xlim = c(-107,-100), ylim = c(18.5,23),
     main = paste0("Puntos de entrenamiento y de control de ",taxon))
points(ocurrencias_entrenamiento$decimalLongitude, ocurrencias_entrenamiento$decimalLatitude, 
       cex = 0.5, col = "red")
points(ocurrencias_control$decimalLongitude, ocurrencias_control$decimalLatitude, 
       cex = 0.5, col = "blue")

puntos_aleatorios_fondo_1 <- randomPoints(bio_vars_mask, n_fondo / 2)
puntos_aleatorios_fondo_2 <- randomPoints(buffer_raster, n_fondo / 2)
puntos_aleatorios_fondo <- rbind(puntos_aleatorios_fondo_1,
                                 puntos_aleatorios_fondo_2)
points(puntos_aleatorios_fondo, col = "gray", cex = 0.5, pch = 3)
```

## Realizar muestreo de valores ambientales

```{r muestreo, cache = TRUE}
x_presencia <- extract(subset(bio_vars, predictors), ocurrencias_entrenamiento[,2:3])
x_control <- extract(subset(bio_vars, predictors), ocurrencias_control[,2:3])
x_fondo <- extract(subset(bio_vars, predictors), puntos_aleatorios_fondo)

# generar vector de presincias (1) y valores del fondo (0)
y <- c(rep(1, dim(ocurrencias_entrenamiento)[1]),
       rep(0, dim(puntos_aleatorios_fondo)[1]))
x <- rbind(x_presencia, x_fondo)

specie_entrenamiento <- as.data.frame(cbind(x,y))
dim(specie_entrenamiento)

#str(specie_entrenamiento)
head(specie_entrenamiento)

y_c <- c(rep(1, dim(ocurrencias_control)[1]),
       rep(0, dim(puntos_aleatorios_fondo)[1]))
x_c <- rbind(x_control, x_fondo)

specie_control <- as.data.frame(cbind(x_c,y_c))
dim(specie_control)
```

## Construir el modelo MAXENT

```{r modelo, cache = TRUE}
jar <- paste(system.file(package='dismo'), '/java/maxent.jar', sep='')
modelos <- list()
reg_values <- c(0.8,0.9,1,1.1,1.5,2,2.5,3,3.5,4,5,6)
#reg_values <- c(1,3)
n_modelos <- length(reg_values)


for (i in 1:n_modelos) {
  modelo_maxent <- maxent(
                        x = specie_entrenamiento[predictors],
                        p = specie_entrenamiento[,"y"],
                        silent = FALSE,
                        args=c(
                          'removeDuplicates=TRUE', 
                          'jackknife=FALSE',
                          'responsecurves=TRUE',
                          'threads=3',
                          'linear=TRUE',
                          'threshold=FALSE',
                          'quadratic=TRUE',
                          'hinge=TRUE',
                          'product=FALSE',
                          paste0('betamultiplier=',reg_values[i])
                        ))
    modelos[[i]] <- modelo_maxent
  
  
}
saveRDS(modelos, paste0(file_prefix,
                              "_",taxon_,".rds"))
```

```{r maxent-specific, cache=TRUE}
# prediccion_entrenamiento <- predict(object = modelos[[1]], 
#                                     x = specie_entrenamiento[predictors])
# prediccion_entrenamiento <- data.frame(y = prediccion_entrenamiento)
# 
## biblioteca https://rdrr.io/github/johnbaums/rmaxent/
prediccion_raw_list <- list()

for (i in 1:n_modelos) {
    print(parse_lambdas(modelos[[i]]))
    non_zero_features <- n_features(modelos[[i]])
    print(non_zero_features)
    
    prediccion_raw_list[[i]] <- rmaxent::project(modelos[[i]],
                        subset(bio_vars, predictors),
                        quiet=TRUE)$prediction_raw
}

prediccion_raw <- stack(prediccion_raw_list)
names(prediccion_raw) <- paste0("r",reg_values)
plot(prediccion_raw)
```

```{r best-model}
modelos_ic <- ic(prediccion_raw,
    ocurrencias_entrenamiento[,2:3],
    modelos[1:n_modelos])
modelos_ic_df <- as.data.frame(modelos_ic)
modelos_ic_df
#str(modelos_ic_df)
best_index <- which.min(modelos_ic_df$AICc)
print(paste("Número de mejor modelo: ", best_index))

if (is.na(best_index)) {
  best_index <- 1
}

# ## biblioteca https://jamiemkass.github.io/ENMeval/articles/ENMeval-2.0-vignette.html
# library(ENMeval)
# aic.maxent(prediccion_entrenamiento, non_zero_features, p = NULL)
# #r2_pr <- data.frame(y = extract(r2, ocurrencias_entrenamiento[,2:3]))
# #aic.maxent(r2_pr, non_zero_features, p = NULL)
```


```{r grafica-aic}
max_AICc <- max(modelos_ic_df$AICc)
min_AICc <- min(modelos_ic_df$AICc)

plot(x = reg_values, y = modelos_ic_df$AICc,
     lwd = 2, col = "blue", type = "l", log = "y",
     ylim = c(0.9 * min_AICc, 1.1 * max_AICc), xlim = c(0.4, 5.1),
     xlab = "coeficiente de regularización", ylab = "criterio")
lines(x = reg_values, y = modelos_ic_df$BIC, col = "red")
lines(x = reg_values, y = modelos_ic_df$AIC, col = "orange")
legend("topright", bty = "n",
       legend = c("AICc","AIC","BIC"),
       col = c("blue","orange","red"),
       lwd = c(2,1,1))
abline(v = reg_values[best_index], lty = 2)
text(x = reg_values[best_index], y = mean(modelos_ic_df$AICc) + 100,
     label = c(paste0("beta = ", reg_values[best_index],"\n",
              "k = ", modelos_ic_df[best_index,"k"])),
     pos = 4)
```


```{r best-model-jack, cache=TRUE}
modelos[[best_index]] <- maxent(
                        x = specie_entrenamiento[predictors],
                        p = specie_entrenamiento[,"y"],
                        silent = FALSE,
                        args=c(
                          'removeDuplicates=TRUE', 
                          'jackknife=TRUE',
                          'responsecurves=TRUE',
                          'threads=3',
                          'linear=TRUE',
                          'threshold=FALSE',
                          'quadratic=TRUE',
                          'hinge=TRUE',
                          'product=FALSE',
                          paste0('betamultiplier=',reg_values[best_index])
                        ))

saveRDS(modelos, paste0(file_prefix,
                              "_",taxon_,".rds"))
```


## Realizar evaluación del mejor modelo (por AICc)

```{r evaluacion}
evaluacion_e <- evaluate(model = modelos[[best_index]],
              p = specie_entrenamiento[specie_entrenamiento$y == 1,predictors], 
              a = specie_entrenamiento[specie_entrenamiento$y == 0,predictors])
evaluacion_e


evaluacion_c <- evaluate(model = modelos[[best_index]],
              p = specie_control[specie_control$y == 1,predictors], 
              a = specie_control[specie_control$y == 0,predictors]
              )
evaluacion_c

threshold(evaluacion_c)
```

## Presentar graficas de evaluación del mejor modelo

```{r graficas-evaluacion}
par(mfcol = c(1,2))

plot(evaluacion_e, 'ROC')
title(c(" "," ","entranaimiento"))
plot(evaluacion_c, 'ROC')
title(c(" "," ","control"))

plot(evaluacion_e, 'TPR')
title(c(" "," ","entranaimiento"))
plot(evaluacion_c, 'TPR')
title(c(" "," ","control"))

plot(evaluacion_e, 'FPR')
title(c(" "," ","entranaimiento"))
plot(evaluacion_c, 'FPR')
title(c(" "," ","control"))

plot(evaluacion_e, 'TNR')
title(c(" "," ","entranaimiento"))
plot(evaluacion_c, 'TNR')
title(c(" "," ","control"))

plot(evaluacion_e, 'FNR')
title(c(" "," ","entranaimiento"))
plot(evaluacion_c, 'FNR')
title(c(" "," ","control"))

plot(evaluacion_e, 'CCR')
title(c(" "," ","entranaimiento"))
plot(evaluacion_c, 'CCR')
title(c(" "," ","control"))

plot(evaluacion_e, 'MCR')
title(c(" "," ","entranaimiento"))
plot(evaluacion_c, 'MCR')
title(c(" "," ","control"))

plot(evaluacion_e, 'PPP')
title(c(" "," ","entranaimiento"))
plot(evaluacion_c, 'PPP')
title(c(" "," ","control"))

plot(evaluacion_e, 'NPP')
title(c(" "," ","entranaimiento"))
plot(evaluacion_c, 'NPP')
title(c(" "," ","control"))

plot(evaluacion_e, 'OR')
title(c(" "," ","entranaimiento"))
plot(evaluacion_c, 'OR')
title(c(" "," ","control"))

plot(evaluacion_e, 'kappa')
title(c(" "," ","entranaimiento"))
plot(evaluacion_c, 'kappa')
title(c(" "," ","control"))

boxplot(evaluacion_e, col=c('yellow','pink'), notch=TRUE,
        main = "entrenamiento", ylim = c(0,1))
boxplot(evaluacion_c, col=c('yellow','pink'), notch=TRUE,
        main = "control", ylim = c(0,1))

density(evaluacion_e)
title("entranaimiento")
density(evaluacion_c)
title("control")
abline(v = threshold(evaluacion_c)$spec_sens, lty = 2)
```

## Presentar graficas de contribución de variables (paso lento)

```{r respuesta, fig.width = 12, fig.height = 15}
response(modelos[[best_index]], at = NULL)
```

```{r subsroutines}
maxent.select.contribution <- function (m, n) {
  ## Funcion para estraer contribucion de variables de modelo Maxent
  ## parametros: 
  ## m - objeto del model maxent, 
  ## n - vector de nombres de variables raster
  ## Note: with jackknifing - in order to get variables 3 and 4
  resultados <- m@results
  tabla_resultados <- data.frame(t(rep(NA,5)))
  names(tabla_resultados) <- c('variable','contribution','permutation.importance',
                               'gain.without','gain.only')
  for (i in 1:length(n)) {
    var1_name <- paste0(n[i],'.contribution')
    var1_value <- resultados[var1_name,1]
    var2_name <- paste0(n[i],'.permutation.importance')
    var2_value <- resultados[var2_name,1]
    var3_name <- paste0('Training.gain.without.',n[i])
    var3_value <- resultados[var3_name,1]
    var4_name <- paste0('Training.gain.with.only.',n[i])
    var4_value <- resultados[var4_name,1]    
    tabla_resultados <- rbind(tabla_resultados,
        c(n[i],var1_value,var2_value,var3_value,var4_value))
  }
  tabla_resultados <- tabla_resultados[-1,]
  return(tabla_resultados)
}
```

```{r contribucion-variables, fig.height = 8, fig.width = 8}
nombres_variables <- names(subset(bio_vars, predictors))

contribuciones_variables <- maxent.select.contribution(modelos[[best_index]],
                                                      nombres_variables)
contr_max <- max(as.numeric(c(contribuciones_variables[,2],
                              contribuciones_variables[,3])))
gain_max <- max(as.numeric(c(contribuciones_variables[,4],
                              contribuciones_variables[,5])))

par(mfcol = c(1,2), mar = c(5.1, 4.1, 6.1, 2.1))

dotchart(as.numeric(contribuciones_variables[,2]),
          col='blue', pch=16,
          labels=contribuciones_variables[,1],
          xlim=c(0, 1.02 * contr_max),
          main=c('predictor contribution','and importance in permutations'),
          xlab = "%"
         )

points(as.numeric(contribuciones_variables[,3]),
       seq(predictors),
       col='red', pch=1)

legend("top", inset=c(0,-0.07), xpd = TRUE,
       ncol = 2, bty = "n",
       legend = c("contribution","perm. importance"),
       pch = c(16,1), col = c("blue","red"))

dotchart(as.numeric(contribuciones_variables[,4]),
          col = 'orange', pch=16,
          labels = contribuciones_variables[,1],
          xlim = c(0, 1.02 * gain_max),
          main = 'predictor jackknifing',
          xlab = 'training gain')

points(as.numeric(contribuciones_variables[,5]),
       seq(predictors),
       col='black', pch=1)

legend("top", inset=c(0,-0.07), xpd = TRUE,
       ncol = 2, bty = "n",
       legend = c("excluding predictor","only one predictor"),
       pch = c(16,1), col = c("orange","black"))
```

```{r lambdas}
parse_lambdas(modelos[[best_index]])
```

```{r vif-analisis}
vifcor(specie_entrenamiento[predictors], th = 0.9)

important_predctors <- contribuciones_variables[
                      which(contribuciones_variables[,2] > 2),]
vifcor(specie_entrenamiento[important_predctors$variable], th = 0.9)
```

## Realizar prediccion de idoneidad de habitat y clasificación binaria

```{r prediccion-raster, fig.width = 8, fig.height = 8}
# prediccion <- predict(subset(bio_vars, predictors), modelos[[best_index]], progress='window')

prediccion <- rmaxent::project(modelos[[best_index]],
                        subset(bio_vars, predictors),
                        quiet=TRUE)$prediction_logistic

prediccion <- prediccion * bio_vars_mask
plot(prediccion,
  main = paste0("Probabilidad de presencia estimada de ",taxon,", beta=",reg_values[best_index]),
  xlim = c(-107,-100), ylim = c(18.5,23))
plot(mundo, add = TRUE)
```

## Realizar la clasificación binaria con umbral max spec. sens.

```{r clasificacion-raster, fig.width = 8, fig.height = 8}
especie_reclass_table <- matrix(c(-Inf, threshold(evaluacion_c)$spec_sens, 0,
                                 threshold(evaluacion_c)$spec_sens, Inf, 1),
                               ncol=3, byrow=TRUE)
prediccion_presencia <- reclassify(prediccion, especie_reclass_table)
plot(prediccion_presencia, legend = FALSE,
     main = paste0("Probable presencia binaria (max. sens. spec.) de ",taxon),
     xlim = c(-107,-100), ylim = c(18.5,23))
plot(mundo, add = TRUE)
```

## Determinar variables limitantes

```{r limiting}
lim <- limiting(subset(bio_vars, predictors), modelos[[best_index]])
plot(lim)
#levelplot(lim, col.regions=rainbow) +
#  layer(sp.points(SpatialPoints(ocurrencias[,2:3]), pch=20, col=1))
```



## Guadrar rasters

```{r guardar-rasters}
writeRaster(prediccion,
              filename = paste0("rasters/",file_prefix,"_b",reg_values[best_index],
              "_",taxon_,"_probabilidad_presencia.tif"),
             format="GTiff", datatype = "FLT4S", overwrite = TRUE)

writeRaster(prediccion_presencia,
              filename = paste0("rasters/",file_prefix,"_b",reg_values[best_index],
             "_",taxon_,"_clasificacion_sens_spec.tif"),
             format="GTiff", datatype = "INT2S", overwrite = TRUE)

writeRaster(lim,
              filename = paste0("rasters/",file_prefix,"_b",reg_values[best_index],
              "_",taxon_,"_factores_limitantes.tif"),
             format="GTiff", datatype = "INT2S", overwrite = TRUE)
```



